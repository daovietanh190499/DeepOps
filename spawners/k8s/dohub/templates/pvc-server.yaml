apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: claim-dohub-server
  namespace: dohub
spec:
  storageClassName: volume-dohub-server
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi